import { AppRolesDirective } from './app-roles.directive';

describe('AppRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new AppRolesDirective();
    expect(directive).toBeTruthy();
  });
});
